//caractéres para copiar: 	[] 		, 	8 	, 	() 	, 	|

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
prt(struct persona.pp){
	printf("\nLos datos de nuestro participante son:\n");
	printf("dni: %d", pp.dni);
	printf("\n %s,%s",pp.sNom, pp.sApp);
	printf("\n dni: %d", pp.dni);
	//printf("\n cant(%c)%d",'a', cant_car(pp,'a'));
	return;
	}*/
	
int cant_c(struct persona p, char c)
{
	int cont=0;
	int i;
	char s;
	for(i=0; p.sNom[])
	}
struct persona
{
	int dni;
	char sNom[];
	char sApp[];
	};

prt(struct persona);


int main(int argc, char **argv)
{
	struct a.dni=40.357.251
			strcpy(a.nombre,"carlos");
			strcpy(a.apellido"Varela");
			
			b=a;
			//	prt(a);
	int cant_c(struct persona p, char c)
	
	return 0;
}

